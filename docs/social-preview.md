# Social Preview

The README uses `assets/hero.png` as its visual anchor at `640px`, followed by the project name, value proposition, five compact status badges, language links, and navigation. The core-feature overview and complete specialist-skill directory lead the page; installation and detailed workflow explanations follow.

GitHub's repository social-preview image is a separate surface. If a custom preview is added there, evaluate it at card size, use the repository name as the primary signal, and avoid dense workflow text or unsupported scientific claims.
